# === CORE ===
pandas==2.2.2
numpy==1.26.4
pyyaml==6.0.1
requests==2.32.3
typer==0.12.3
rich==13.7.1
python-dotenv==1.0.1

# === LOGGING & UTILITIES ===
loguru==0.7.2
orjson==3.10.5
tqdm==4.66.4
python-dateutil==2.9.0

# === LANGCHAIN SUITE (Perfectly Compatible Versions) ===
# LangChain main package
langchain==0.3.7

# LangChain core (required)
langchain-core==0.3.19

# Community integrations (contains Ollama integration)
langchain-community==0.3.7

# === PYDANTIC (required by LangChain 0.3.x) ===
pydantic==2.9.2
pydantic_core==2.23.4

# === LANGGRAPH (Optional, NOT USED – but kept for compatibility) ===
# You are NOT using LangGraph in this project.
# But if import happens accidentally, this avoids hard errors.
langgraph==0.2.34

# === RETRIES ===
tenacity==8.3.0

# === TESTING ===
pytest==8.3.2
pytest-cov==5.0.0
